# import inputG, algrtm2, algrtm3

my_file = list(open('graph_sample1.txt', 'r'))
#read edges from file
edge = []
edges = []
for line in my_file:
	edge.append(line)
	edge = [x.strip() for x in edge]
for i in range(len(edge)):
	u, v = edge[i].split()
    edges[(int(u)] = int(v)
    edges[(int(v)] = int(u)

while len(Removed) <= k or #check edge with func:
	v_star = #def in alg2 /3 : select v ∗ = arg min v∈V f (G \ {v ∗ })
	# edges[v*] = 'null' always check if v star is in removed
	Removed.add(v_star)


